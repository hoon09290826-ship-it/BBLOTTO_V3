# 등록일 수정 최종 수정 내역

- 회원 수정 저장 후 입력 폼 초기화 제거
- 저장 후 서버에서 다시 불러온 최신 회원값으로 자동 재선택
- 등록일 `created_at`은 `YYYY-MM-DD` 값으로 정규화해서 전송
- 계약만료일 직접 전송 제거, 서버에서 등록일 + 계약기간으로 자동 계산
- `/app.js`, `/dashboard` 응답에 no-cache 헤더 추가
- 저장 API가 수정된 회원 데이터를 함께 반환하도록 보완

업로드 후 브라우저에서 Ctrl+F5로 새로고침하세요.
